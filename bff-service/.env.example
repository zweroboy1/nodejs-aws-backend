PORT=3000

# Service URLs — key must match the first path segment (uppercased)
# e.g. GET http://localhost:3000/product/... → forwards to PRODUCT value
PRODUCT=https://your-product-service-url.amazonaws.com/prod
CART=https://your-cart-service-url.cloudfront.net
